1. Zabbix-agent 
we insatll Zabbix server and agent then we config the configuration file

2.Software
we install the latest update and epel package 
also we use roles to install a certain packages specified in main.yml
